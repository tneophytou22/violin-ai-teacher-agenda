(function(){'use strict';
/* V13 CURRICULUM BRIDGE — canonical transport layer.
   One-way rule: UI selection -> canonical V2 Lesson store -> explicit studentId -> Lesson.
   The bridge owns Scales UI transport only; Lesson owns Lesson rendering.
*/
const frame=document.getElementById('app');let bound=null,observer=null,lastKey='';
const STORE='VIOLIN_AI_CURRICULUM_SELECTION_V2';
function students(){try{const x=JSON.parse(localStorage.getItem('VIOLIN_AI_AGENDA_V10')||'{}');return Array.isArray(x.students)?x.students:[]}catch(e){return[]}}
function currentStudent(doc){const h=doc.querySelector('.title h2');if(!h)return null;const m=(h.textContent||'').match(/^\s*🎵\s*SCALES\s*·\s*(.+?)\s*$/i);if(!m)return null;const n=m[1].trim();return students().find(s=>String(s.name||s.studentName||'').trim()===n)||null}
function curriculumItems(s){const C=window.parent.VIOLIN_AI_CURRICULUM?.controller;if(C&&typeof C.list==='function'){const a=C.list('scales',Number(s.level)||1,Number(s.term)||1);if(Array.isArray(a)&&a.length)return a}const D=window.parent.VIOLIN_SCALE_CURRICULUM_V1,c=D?.[s.level]?.[Number(s.term)||1]||D?.[s.level]?.[1];if(!c)return[];const groups=[['major','Major Scales'],['minor','Minor Scales'],['arpeggios','Tonic Arpeggios'],['dominant7','Dominant 7th'],['diminished7','Diminished 7th'],['chromatic','Chromatic Scales'],['doubleStops','Double Stops'],['oneString','One-String Scales']],out=[];groups.forEach(([k,cat])=>(Array.isArray(c[k])?c[k]:[c[k]]).forEach(x=>x&&out.push({id:'scale-'+s.level+'-'+(Number(s.term)||1)+'-'+out.length,curriculum:'scales',category:cat,title:String(x),level:Number(s.level)||1,term:Number(s.term)||1,requirements:{tempo:c.tempo,octaves:c.octaves,bowing:c.bowing,articulation:c.articulation,rhythmicGroups:c.rhythmicGroups,accents:c.accents,dynamics:c.dynamics,positions:c.positions},source:'scale-curriculum-data.js'})));return out}
function remove(d){d.getElementById('v13-curriculum-scales')?.remove();lastKey=''}
function saveCanonical(p){let db={};try{db=JSON.parse(localStorage.getItem(STORE)||'{}')||{}}catch(e){db={}}const id=String(p.studentId),old=db[id]&&typeof db[id]==='object'?db[id]:{};old.lesson=old.lesson&&typeof old.lesson==='object'?old.lesson:{};old.homework=old.homework&&typeof old.homework==='object'?old.homework:{};if(p.kind==='lesson')old.lesson.scales={studentId:id,studentName:p.studentName,level:p.level,term:p.term,kind:'lesson',curriculum:'scales',items:Array.isArray(p.items)?p.items:[],updatedAt:new Date().toISOString(),source:'V13 Scales Bridge'};else old.homework.scales={studentId:id,studentName:p.studentName,level:p.level,term:p.term,kind:'homework',curriculum:'scales',items:Array.isArray(p.items)?p.items:[],updatedAt:new Date().toISOString(),source:'V13 Scales Bridge'};db[id]=old;localStorage.setItem(STORE,JSON.stringify(db));if(p.kind==='lesson'&&window.parent.ViolinAI?.State?.scales?.setSelected)window.parent.ViolinAI.State.scales.setSelected(id,p.items);return db[id]}
function navigateToLesson(s){const url=new URL('./lesson-v13.html',window.location.href);url.searchParams.set('studentId',String(s.id));url.searchParams.set('v','scales-to-lesson-'+Date.now());window.location.assign(url.href)}
function save(kind,s,it,host){const chosen=it.filter((x,i)=>host.querySelector('[data-i="'+i+'"]')?.checked);if(!chosen.length)return alert('Select at least one scale.');const p={studentId:String(s.id),studentName:String(s.name||s.studentName||'Student'),level:Number(s.level)||1,term:Number(s.term)||1,kind,curriculum:'scales',items:chosen,createdAt:new Date().toISOString()};try{saveCanonical(p);const C=window.parent.VIOLIN_AI_CURRICULUM?.controller;if(C&&typeof C.select==='function')C.select(p);localStorage.setItem('VIOLIN_AI_CURRENT_SELECTION_V1',JSON.stringify(p));localStorage.setItem('VIOLIN_AI_LESSON_SELECTION_V1',JSON.stringify(p));const db=JSON.parse(localStorage.getItem(STORE)||'{}'),saved=db[String(s.id)]?.lesson?.scales;if(kind==='lesson'&&(!saved||!Array.isArray(saved.items)||saved.items.length!==chosen.length))throw new Error('Canonical selection verification failed');if(kind==='lesson')navigateToLesson(s);else alert('Selected scales added to Homework.')}catch(e){console.error('[V13 Scales] selection save failed',e);alert('Could not save the scale selection. Please refresh and try again.')}}
function render(d){const s=currentStudent(d);if(!s){remove(d);return}const it=curriculumItems(s);if(!it.length){remove(d);return}const key=String(s.id||s.name)+'|'+String(s.level)+'|'+String(Number(s.term)||1);let host=d.getElementById('v13-curriculum-scales');if(host&&lastKey===key)return;if(!host){const tabs=d.querySelector('.tabs');if(!tabs)return;host=d.createElement('section');host.id='v13-curriculum-scales';tabs.insertAdjacentElement('afterend',host)}lastKey=key;let h='<div class="v13-scale-box"><h3>🎼 Curriculum Scales</h3><div class="v13-scale-sub">Level '+String(s.level)+' · Term '+String(Number(s.term)||1)+'</div><div class="v13-scale-grid">';const groups={};it.forEach((x,i)=>(groups[x.category]??=[]).push([x,i]));Object.entries(groups).forEach(([cat,a])=>{h+='<div class="v13-scale-cat"><b>'+cat+'</b>';a.forEach(([x,i])=>h+='<label><input type="checkbox" data-i="'+i+'"><span>'+String(x.title||x.name||x)+'</span></label>');h+='</div>'});h+='</div><div class="v13-scale-actions"><button type="button" data-a="lesson">🎓 Add selected to Lesson</button><button type="button" data-a="homework">🏠 Add selected to Homework</button></div></div>';host.innerHTML=h;if(!d.getElementById('v13-scale-css')){const st=d.createElement('style');st.id='v13-scale-css';st.textContent='.v13-scale-box{margin:12px 0;padding:14px;border:1px solid #dfd4ff;border-radius:16px;background:#faf8ff;color:#24344c}.v13-scale-box h3{margin:0 0 3px;font-size:19px}.v13-scale-sub{font-size:12px;color:#687487;margin-bottom:10px}.v13-scale-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.v13-scale-cat{border:1px solid #e4e0ef;border-radius:10px;background:#fff;padding:9px}.v13-scale-cat>b{display:block;margin-bottom:5px;font-size:12px}.v13-scale-cat label{display:flex;gap:7px;align-items:flex-start;margin:5px 0;font-size:12px;font-weight:800}.v13-scale-cat input{width:17px;height:17px;margin:0}.v13-scale-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.v13-scale-actions button{border:0;border-radius:9px;padding:9px 12px;font-weight:800;cursor:pointer}.v13-scale-actions button:first-child{background:#24344c;color:#fff}.v13-scale-actions button:last-child{background:#64c6a3;color:#12372c}@media(max-width:650px){.v13-scale-grid{grid-template-columns:1fr}}';d.head.appendChild(st)}host.querySelector('[data-a="lesson"]').onclick=()=>save('lesson',s,it,host);host.querySelector('[data-a="homework"]').onclick=()=>save('homework',s,it,host)}
function bind(){const d=frame?.contentDocument;if(!d?.body||bound===d)return;bound=d;if(observer)observer.disconnect();observer=new MutationObserver(()=>{const s=currentStudent(d);if(s)render(d);else remove(d)});observer.observe(d.body,{childList:true,subtree:true});render(d)}
frame?.addEventListener('load',()=>{bound=null;lastKey='';bind()});if(frame?.contentDocument)bind();window.VIOLIN_AI_CURRICULUM_BRIDGE={version:'15.0',sync:bind};
})();